---
layout: null
---
[
  {% for report in site.reports %}
    {
      "id": {{ report.name | split: "." | first | jsonify }},
      "title": {{ report.title | jsonify }},
      "description": {{ report.description | jsonify }},
      "query": {{ report.query | jsonify }}
    }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]